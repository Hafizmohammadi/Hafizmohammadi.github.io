//JavaScript Basics:

// Math Object:- built-in object that has properties and methods for mathematical constants and functions

//z = Math.round(x);
//z = Math.floor(x);
//z = Math.ceil(x);
//z = Math.trunc(x);
//z = Math.pow(x, y);
//z = Math.sqrt(y);
//z = Math.log(9);
// z = Math.sin(y);
// z = Math.cos(y);
// z = Math.tan(y);
// z = Math.max(5,10,14,3,7,1);
// z = Math.min(5,10,14,3,7,1);
// z = Math.abs(x);
// z = Math.sign(x);

//...........................................

// User Input

//how to accept user input
//1. Easy way: window prompt
//2. professional way = Html textbox

//.................

// Random Number Generation

// Math.random() returns a random number between 0 (inclusive) and 1 (exclusive)
// To get a random number between min and max (inclusive):
// Math.floor(Math.random() * (max - min + 1)) + min

//..............

// IF Statements: if a condition is true, execute a block of code. if not, do something else

  //................

  //.checked:- property that determines the checked state of an HTML checkbox or radio button element.

  //ternary operator:- shortcut for if statement
  // condition ? expr1 : expr2

  //.................

  // Switch statements:- used to perform different actions based on different conditions

  //..................

  // string methods:- manipulate and work with strings
  //chatAt():- returns the character at a specified index in a string
  //indexOf():- returns the index of the first occurrence of a specified value in a string
  //lastIndexOf():- returns the index of the last occurrence of a specified value in a string
  //slice():- extracts a section of a string and returns it as a new string
  //toUpperCase():- converts a string to uppercase letters
  //toLowerCase():- converts a string to lowercase letters
  //replace():- replaces a specified value with another value in a string
  //replaceAll():- replaces all occurrences of a specified value with another value in a string
  //repeat():- repeats a string a specified number of times
  //startsWith():- checks if a string starts with a specified value
  //endsWith():- checks if a string ends with a specified value
  //includes():- checks if a string contains a specified value
  //padStart():- pads a string with another string at the beginning
  //padEnd():- pads a string with another string at the end
  //substring():- extracts characters from a string between two specified indices
  //substr():- extracts a specified number of characters from a string, starting at a specified index

  //.........................

  // Method chaining:- calling multiple methods on the same object in a single line of code

  // Logical Operators:- used to combine multiple conditions
  // && (AND):- returns true if both conditions are true
  // || (OR):- returns true if at least one condition is true
  // ! (NOT):- reverses the value of a condition

  //.........................

  //= Assignment Operators:- used to assign values to variables
  //== Equal to:- compares two values for equality (type conversion allowed)
  //=== Strict equal to:- compares two values for equality (no type conversion)
  //!= Not equal to:- compares two values for inequality (type conversion allowed)
  //!== Strict not equal to:- compares two values for inequality (no type conversion)
  //> Greater than:- checks if the left value is greater than the right value
  //< Less than:- checks if the left value is less than the right value
  //>= Greater than or equal to:- checks if the left value is greater than or equal to the right value
  //<= Less than or equal to:- checks if the left value is less than or equal to the right value

  //......................

  //Loops:- used to repeat a block of code multiple times
  //for loop:- used to execute a block of code a specified number of times
  //while loop:- used to execute a block of code as long as a specified condition is true
  //do...while loop:- used to execute a block of code at least once, and then repeatedly as long as a specified condition is true
  //for...in loop:- used to iterate over the properties of an object
  //for...of loop:- used to iterate over the values of an iterable object

  //.......................

  // Functions:- used to group a block of code that performs a specific task
  // function functionName(parameters) {
  //   // code to be executed
  // }
  // Function Parameters vs Arguments
// Parameters are the names listed in the function definition
// Arguments are the real values passed to the function
  // Return Statement:- used to return a value from a function and exit the function

  //variables scope:- determines the accessibility of variables
  // Global Scope:- variables declared outside of any function or block
  // Local Scope:- variables declared inside a function or block

  //.......................

  // Arrays:- used to store multiple values in a single variable
  // Array Methods:-
  // push():- adds one or more elements to the end of an array
